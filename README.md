# Books vault

Obsidian vault for **reader-written** book summaries and chapter navigation. Project conventions live in [AGENTS.md](AGENTS.md).

## Open this vault in Obsidian

1. Clone or download this repository to your local machine.
2. Open Obsidian and choose **Open folder as vault**.
3. Select this repository folder as the vault root.
4. When Obsidian asks about community plugins, allow them for this vault.
5. Open **Settings → Community plugins** and make sure `Books Reading Progress` is enabled.
6. Optional plugins:
   - `Hover Editor` if you want resizable hover previews for explainer notes
   - `Excalidraw` if you want to open the diagram files
   - `Editing Toolbar` only if you plan to edit notes inside Obsidian
7. Open [Home.md](Home.md). This is the main entry page for reading.

## Recommended Obsidian settings

- Use `Reading view` as the default note view.
- Keep `Outline` open so you can jump between concept cards quickly.
- Enable `Page Preview` for hover previews on chapter links and explainer links.
- Use `Hover Editor` only if you want larger, draggable previews than the built-in `Page Preview`.
- If a custom plugin or style update does not appear immediately, run `Developer: Reload app`.

## How to read this vault

- Start from [Home.md](Home.md) or [books/taleb-antifragile-2012/book-overview.md](books/taleb-antifragile-2012/book-overview.md).
- Open a chapter note in `books/taleb-antifragile-2012/chapters/`.
- Use the `Reading status` block at the top of the chapter to mark it as `Read` or `Unread`.
- The `Chapter index` table on [Home.md](Home.md) uses the same local progress state, so you can also change status there.
- `English` and `Chinese` explainer links open deeper concept notes. On desktop, `Page Preview` or `Hover Editor` makes these much easier to use.

## License and third-party content

- **Repository text** (summaries, notes, structure, custom plugin code): see [LICENSE](LICENSE).
- **Commercial books** are **not** distributed here. Page and chapter locators refer to standard editions so you can check claims against a copy you **lawfully** own.
- Summaries are independent study aids; they are not affiliated with or endorsed by authors or publishers.

## Antifragile

See [books/taleb-antifragile-2012/README.md](books/taleb-antifragile-2012/README.md) and [books/taleb-antifragile-2012/source/README.md](books/taleb-antifragile-2012/source/README.md) for how local source files work.
